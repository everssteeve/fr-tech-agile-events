---
import '../styles/global.css';
import { href } from '../lib/data';

interface Props {
  title: string;
  description?: string;
}
const { title, description = 'Synthèses des retours participants des événements tech et agiles français.' } = Astro.props;
const nav = [
  { path: '', label: 'Calendrier' },
  { path: 'mois/', label: 'Synthèses du mois' },
  { path: 'evenements/', label: 'Événements' },
  { path: 'methode/', label: 'Méthode' },
];
const current = Astro.url.pathname;
const isActive = (path: string) => {
  const target = href(path);
  if (path === '') return current === target || current === `${target}/` || current.startsWith(href('annee/'));
  return current.startsWith(target);
};
const onSearch = current.startsWith(href('recherche/'));
---
<!doctype html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <link rel="icon" href={href('favicon.svg')} type="image/svg+xml" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,500;12..96,700;12..96,800&family=Atkinson+Hyperlegible:ital,wght@0,400;0,700;1,400&family=JetBrains+Mono:wght@400;600&display=swap" />
  </head>
  <body>
    <a class="skip" href="#contenu">Aller au contenu</a>
    <header class="site" id="site-header">
      <div class="wrap bar">
        <a class="brand" href={href('')}>Retours de conf'<span>tech &amp; agile · France</span></a>
        <div class="actions">
          <a class={`search${onSearch ? ' on' : ''}`} href={href('recherche/')} aria-current={onSearch ? 'page' : undefined} title="Rechercher (touche /)">
            <svg viewBox="0 0 20 20" width="18" height="18" aria-hidden="true"><circle cx="8.5" cy="8.5" r="5.5" fill="none" stroke="currentColor" stroke-width="2"/><path d="M13 13l4.5 4.5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
            <span class="search-label">Rechercher</span>
          </a>
          <button type="button" class="menu-btn" aria-expanded="false" aria-controls="main-nav">
            <svg viewBox="0 0 20 20" width="18" height="18" aria-hidden="true"><path d="M3 5h14M3 10h14M3 15h14" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
            Menu
          </button>
        </div>
        <nav id="main-nav" aria-label="Navigation principale">
          {nav.map((n) => <a href={href(n.path)} aria-current={isActive(n.path) ? 'page' : undefined}>{n.label}</a>)}
        </nav>
      </div>
    </header>
    <main id="contenu" tabindex="-1">
      <slot />
    </main>
    <footer class="site">
      <div class="wrap foot">
        <p>Synthèses produites à partir de retours publics (LinkedIn, blogs, vidéos), mises à jour chaque nuit. <a href={href('methode/')}>Comment c'est fait</a></p>
        <p><a href="https://github.com/everssteeve/fr-tech-agile-events">Code source</a></p>
      </div>
    </footer>
    <button type="button" class="to-top" id="to-top" hidden aria-label="Revenir en haut de la page">
      <svg viewBox="0 0 20 20" width="18" height="18" aria-hidden="true"><path d="M10 16V4M4.5 9.5L10 4l5.5 5.5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
      <span>Haut</span>
    </button>
    <script>
      const header = document.getElementById('site-header')!;
      const root = document.documentElement;
      // Expose the header height so sticky bars and scroll margins stack under it.
      new ResizeObserver(() => root.style.setProperty('--hdr-h', `${header.offsetHeight}px`)).observe(header);

      const btn = header.querySelector<HTMLButtonElement>('.menu-btn')!;
      const nav = document.getElementById('main-nav')!;
      const setOpen = (open: boolean) => {
        btn.setAttribute('aria-expanded', String(open));
        nav.classList.toggle('open', open);
      };
      btn.addEventListener('click', () => setOpen(btn.getAttribute('aria-expanded') !== 'true'));
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') setOpen(false);
        const t = e.target as HTMLElement;
        const typing = t.isContentEditable || ['INPUT', 'TEXTAREA', 'SELECT'].includes(t.tagName);
        if (e.key === '/' && !typing && !e.metaKey && !e.ctrlKey) {
          const search = document.querySelector<HTMLInputElement>('#q');
          e.preventDefault();
          if (search) search.focus();
          else location.href = header.querySelector<HTMLAnchorElement>('.search')!.href;
        }
      });

      // Back to top: shown once the reader is more than a screen away from the top.
      const toTop = document.getElementById('to-top')!;
      const onScroll = () => { toTop.hidden = scrollY < innerHeight * 1.2; };
      addEventListener('scroll', onScroll, { passive: true });
      onScroll();
      toTop.addEventListener('click', () => {
        scrollTo({ top: 0, behavior: 'instant' });
        header.querySelector<HTMLAnchorElement>('.brand')!.focus();
      });
    </script>
  </body>
</html>
<style>
  .skip { position: absolute; left: 12px; top: -60px; z-index: 30; background: var(--ink); color: var(--bg); padding: 10px 14px; border-radius: 6px; text-decoration: none; }
  .skip:focus { top: 8px; }
  header.site { border-bottom: 1px solid var(--line); background: var(--surface); position: sticky; top: 0; z-index: 20; }
  .bar { display: flex; align-items: center; gap: 8px 24px; padding-block: 8px; }
  .brand { font-family: var(--font-d); font-weight: 800; font-size: 20px; text-decoration: none; display: flex; align-items: baseline; gap: 10px; margin-right: auto; padding-block: 6px; }
  .brand span { font-family: var(--font-m); font-weight: 400; font-size: 11.5px; color: var(--muted); letter-spacing: .06em; text-transform: uppercase; }
  nav { display: flex; gap: 4px; order: 2; }
  nav a { text-decoration: none; color: var(--muted); font-size: 15px; padding: 10px 10px; border-radius: 6px; position: relative; }
  nav a:hover { color: var(--ink); background: var(--surface-2); }
  nav a[aria-current="page"] { color: var(--ink); }
  nav a[aria-current="page"]::after { content: ""; position: absolute; left: 10px; right: 10px; bottom: 4px; height: 2px; background: var(--accent); }
  .actions { display: flex; align-items: center; gap: 6px; order: 3; }
  .search, .menu-btn { display: inline-flex; align-items: center; gap: 6px; min-height: 40px; padding: 0 12px; border: 1px solid var(--line); border-radius: 999px; background: var(--surface); color: var(--muted); font: inherit; font-size: 14px; text-decoration: none; cursor: pointer; }
  .search:hover, .menu-btn:hover, .search.on { color: var(--ink); border-color: var(--muted); }
  .menu-btn { display: none; }
  @media (max-width: 900px) {
    .brand span { display: none; }
    .menu-btn { display: inline-flex; }
    .search-label { display: none; }
    .search { padding: 0; width: 44px; justify-content: center; }
    .search, .menu-btn { min-height: 44px; }
    .bar { flex-wrap: wrap; }
    nav { display: none; order: 4; flex-basis: 100%; flex-direction: column; gap: 0; padding-bottom: 8px; }
    nav.open { display: flex; }
    nav a { padding: 12px 8px; font-size: 16px; }
    nav a[aria-current="page"]::after { left: 8px; right: auto; width: 24px; bottom: 6px; }
  }
  main { padding-block: 32px 64px; }
  main:focus { outline: none; }
  footer.site { border-top: 1px solid var(--line); padding-block: 20px 32px; font-size: 13.5px; color: var(--faint); }
  .foot { display: flex; flex-wrap: wrap; justify-content: space-between; gap: 8px 24px; }
  .foot p { margin: 0; }
  .to-top { position: fixed; right: 16px; bottom: calc(16px + env(safe-area-inset-bottom, 0px)); z-index: 25; display: inline-flex; align-items: center; gap: 6px; min-height: 44px; padding: 0 16px; border-radius: 999px; border: 1px solid var(--ink); background: var(--ink); color: var(--bg); font: inherit; font-size: 14px; cursor: pointer; }
  .to-top:hover { background: var(--accent); border-color: var(--accent); color: var(--on-accent); }
</style>
